# SYNC_PianoRoll 作業ノート

## 目的

MIDI等の音楽データを読み、AviUtl2上で鍵盤とノートを音楽へ同期して描画する。
タイムラインへ直接配置できる単体の映像フィルタープラグインを主構成とし、画面全体へ描画する。

## 現在の最小構成

- `SYNC_PianoRoll_Filter.dpr/.dproj`: 単体映像フィルターとして音楽ファイルを解析し、選択した表示実装へ描画を委譲する。成果物は `.auf2`。
- `SYNC_PianoRoll_Input.dpr/.dproj`: 旧2プラグイン構成の互換確認用。標準変形を使う構成のベース候補として同じフォルダーへ残す。
- `Source\Plugin\Filter\SYNC_PianoRoll_FilterPlugin.pas`: 単体フィルター登録、設定取得、表示実装の選択を担当する。
- `Source\Plugin\Input\SYNC_PianoRoll_InputPlugin.pas`: 旧仮想素材構成。現在のFilterからは参照しない。
- `Source\Common\Timeline\SYNC_PianoRoll_Time.pas`: 単体フィルターへ渡されたローカル再生時刻を取得する。
- `Source\Common\Timeline\SYNC_PianoRoll_FrameShared.pas`: 旧Input用共有処理。現在のFilterからは参照しない。
- `Source\Common\Timeline\SYNC_PianoRoll_ContextManager.pas`: 旧共有フレーム構成の回帰確認用。現在のFilterからは参照しない。
- `Source\Common\Data\SYNC_PianoRoll_MusicData.pas`: 元音楽ファイルの解析結果を描画用の読み取り専用スナップショットとして共有する。
- `Source\Common\Color\SYNC_PianoRoll_Colors.pas`: RGBA色型、描画共通パレット、単色と配色バリエーションを定義する。
- `Source\Common\Layout\SYNC_PianoRoll_DisplayTypes.pas`: 縦横に依存しない表示設定と表示実装の共通契約を定義する。
- `Source\Common\Render\SYNC_PianoRoll_RGBA.pas`: 表示タイプに依存しないRGBAキャンバスと矩形描画を提供する。
- `Source\Common\Render\SYNC_PianoRoll_Renderer.pas`: 単体フィルターへ渡された縦横寸法で描画バッファを管理し、選択された表示実装を呼び出す。
- `Source\Display\Vertical\SYNC_PianoRoll_VerticalDisplay.pas`: 時間方向を下向き、音階方向を左から右へ割り当てる縦表示を担当する。
- `Source\Display\Horizontal\SYNC_PianoRoll_HorizontalDisplay.pas`: 時間方向を右向き、音階方向を上から下へ割り当てる横表示を担当する。
- `Source\Lib\AviUtl2InputTypes.pas`: 入力プラグインSDKの最小Delphi ABI定義。
- `Source\Lib\AviUtl2FilterTypes.pas`: フィルタープラグインSDKの最小Delphi ABI定義。
- `Source\Lib\SharedMemoryBase.pas`: 名前付き共有メモリの最小基礎処理。
- `Source\Lib\SongReader`: `SYNC_Motion`から導入したMIDI、UST、VSQX、MusicXML、MuseScore解析処理。
- `Tests\SYNC_PianoRoll_FrameContextTests.dpr`: 共有メモリとオブジェクト別フレーム補間の回帰テスト。
- `Tests\SYNC_PianoRoll_MusicDataTests.dpr`: 最小MIDIの直接解析とキャッシュ更新の回帰テスト。
- `Tests\SYNC_PianoRoll_PianoKeyTests.dpr`: 白鍵・黒鍵判定と白鍵単位の音階方向位置を検証する。
- `Tests\SYNC_PianoRoll_PluginLoadTests.dpr`: Filter登録表と音楽ファイル項目のロードテスト。
- `Tests\SYNC_PianoRoll_FilterSettingsTests.dpr`: 実Filter DLLへ音域値を渡し、描画結果の変化を検証する。
- `Tests\SYNC_PianoRoll_RenderTests.dpr`: RGBA出力、ノート矩形、出力サイズ変更を検証する描画テスト。
- `Tests\SYNC_PianoRoll_HorizontalRenderTests.dpr`: 横表示の座標、鍵盤、レーン、拍線、ノート描画を検証する。
- `Tests\SYNC_PianoRoll_StandaloneTimeTests.dpr`: 単体フィルターへ渡されたローカル時刻の取得を検証する。

AviUtl2上の識別名は次の値で固定する。

```text
SYNC_ピアノロール_Input
SYNC_ピアノロール_Filter
```

入力用の仮想素材拡張子は `.syncpianoroll` とし、次のファイル名形式を解釈する。

```text
Width_Height_MaxSec_Fps_Scale.syncpianoroll
```

値を省略または解釈できない場合は、幅1、高さ1、3600秒、30fps、scale 1を使う。

## 単体フィルターの座標と時刻

Filter登録は`FILTER_FLAG_VIDEO`だけを使用する。`FILTER_FLAG_FILTER`は付けず、既存の画像オブジェクトへ
追加するフィルターではなく、タイムラインへ直接配置する単体映像フィルターとして扱う。

描画座標の幅と高さは、映像コールバックの`Object_.Width`と`Object_.Height`をそのまま使う。
単体フィルターではこの縦横値に画面全体の描画範囲が渡されるため、別の素材寸法や固定解像度を基準にしない。
縦表示と横表示は同じRGBAキャンバス寸法を受け取り、それぞれの時間方向と音階方向へ座標を割り当てる。

音楽との同期時刻は`Object_.Time`を使う。単体フィルターの配置先頭を音楽データの0秒とし、
Inputプラグイン、共有メモリ、シーン絶対フレームをFilterの動作条件にしない。

## 単体フィルター方式の既知の問題

単体フィルターには変形対象となるベース画像オブジェクトがなく、シーン全体のRGBA画像をFilter自身が
直接生成している。そのため、AviUtl2の標準項目による単純な位置移動、回転、拡大率、透明度等を
通常の画像オブジェクトと同じ方法では適用できない。

これらの標準変形を利用するには、同じプラグインフォルダーに存在する
`SYNC_PianoRoll_Input`をベース画像オブジェクトとして配置し、そのオブジェクトへ
`SYNC_ピアノロール_Filter`を追加する構成へ戻す必要がある。単体Filter方式と
Input＋Filter方式のどちらを正式構成にするかは未決定とし、この時点ではコード対応を行わない。

## 旧Syncroh2の参照実装

ピアノロール機能の仕様と実装を調査する際は、次の旧`Syncroh2`資産を参照する。

```text
D:\DelphiProg\test\Syncroh2\Syncroh2_Filter_PianoRoll.dpr
D:\DelphiProg\test\Syncroh2\Plugin_Filter\PianoRoll
D:\DelphiProg\test\Syncroh2\Plugin_Extension\Music\PianoRoll
```

- `Syncroh2_Filter_PianoRoll.dpr`: 旧ピアノロールフィルターのプロジェクト構成と使用ユニットを確認する。
- `Plugin_Filter\PianoRoll`: フィルター登録、設定項目、ピアノロール描画処理を確認する。
- `Plugin_Extension\Music\PianoRoll`: 音楽データからピアノロール表示へ変換する処理を確認する。
- 旧構成をそのままコピーせず、現在のInput／Filter構成に必要な処理と依存ユニットだけを選別して取り込む。
- 現在のAviUtl2 SDK ABI、安全方針、並列呼び出し、キャッシュ設計を優先し、旧実装との差異を確認して移植する。

## 旧実装からの重要な変更

旧`Syncroh2`では、事前に生成または保存されたINIファイルを介してピアノロール用データを参照していた。
`SYNC_PianoRoll`ではINIファイルを入力元とせず、MIDI等の元音楽ファイルをFilterから直接参照して解析する。

- 利用者は中間INIファイルを用意せず、Filterの設定項目で元音楽ファイルを指定する。
- 対応形式はSongReaderの採用範囲を決めた後に確定し、Filterのファイル選択候補へ列挙する。
- 解析結果は元ファイルの正規化した絶対パス、更新日時、ファイルサイズ等を基準にキャッシュする。
- 同じ音楽ファイルを複数オブジェクトが参照する場合は、解析結果を読み取り専用で共有する。
- 元ファイルの不存在、未知形式、破損、解析失敗時は例外をAviUtl2へ漏らさず、安全に無描画または無加工で通す。
- 旧INIの構造や保存方式との互換性は前提にせず、必要な表示設定だけを新しい構成で定義する。

## 作り直しと再利用の方針

旧`Syncroh2`のピアノロール全体をそのまま移植しない。新しい構造で作り直し、単独で責務を説明でき、
現在の設計へ無理なく適合できる処理だけを選別して再利用する。旧実装は部品の参照元、動作見本、
仕様確認用として残し、新版のクラス構造やデータ経路の基礎にはしない。

再利用候補は次のとおり。

- SongReaderによる元音楽ファイルの解析。
- MIDIキー、音階、白鍵、黒鍵等の判定。
- トラック別、音階別、階名別の配色計算。
- ノート、鍵盤、拍線等の座標計算式。
- 実際の利用で必要性が確認された初期値、範囲、描画順序。

次の処理は新しい設計で作る。

- Input／Filter間の共有メモリと時刻同期。
- 読み取り専用音楽データ、キャッシュ、並列呼び出し対策。
- 表示タイプを選択し、専用処理へ委譲する仕組み。
- レイアウト計算とRGBA描画の分離。
- AviUtl2の設定項目と安全なコールバック境界。
- 表示設定の保持方法と、将来必要になる永続データの形式。

旧INIを中心としたデータ経路、RTTI／INI保存クラス、VCLフォーム、`TBitmap`依存の描画、
旧共有メモリ、旧コンテキスト管理、複数の責務が混在した巨大ユニットは移植しない。

## ソースフォルダーの分離方針

旧実装では共通処理と表示タイプ固有処理の置き場所が分かりにくくなったため、今後は責務と再利用範囲を
フォルダーで明示する。共通処理から縦表示や横表示の実装を参照せず、各表示タイプから共通処理を利用する
一方向の依存関係とする。表示タイプ同士は互いを参照しない。

次の構成を基本とする。Input／Filter入口、データ、時刻同期、表示契約、RGBA描画、縦表示は
2026-07-24にこの構成へ移動済み。`Color`も共通パレットの実装時に追加済み。
`Horizontal`は2026-07-27に独立した横表示の実装とともに追加した。

```text
Source
├─ Plugin
│  ├─ Input
│  └─ Filter
├─ Common
│  ├─ Data
│  ├─ Timeline
│  ├─ Layout
│  ├─ Render
│  └─ Color
├─ Display
│  ├─ Vertical
│  └─ Horizontal
└─ Lib
   ├─ SongReader
   └─ AviUtl2 SDK／汎用基礎処理
```

- `Source\Plugin`: AviUtl2への登録、コールバック、設定値の受け渡しだけを担当する。
- `Source\Common\Data`: 読み取り専用音楽データ、ノート、拍、トラック、キャッシュを置く。
- `Source\Common\Timeline`: 共有フレーム、オブジェクト別コンテキスト、時刻変換を置く。
- `Source\Common\Layout`: 表示タイプが実装する共通契約、座標、矩形等の値型を置く。
- `Source\Common\Render`: RGBAバッファ、線、矩形、クリップ等の表示タイプに依存しない描画処理を置く。
- `Source\Common\Color`: トラック、音階、階名等の共通配色処理を置く。
- `Source\Display\Vertical`: 縦表示だけのレイアウト、鍵盤、ノート、拍線、発音表示を置く。
- `Source\Display\Horizontal`: 横表示だけのレイアウト、鍵盤、ノート、拍線、発音表示を置く。
- `Source\Lib`: SongReader、AviUtl2 SDK定義、共有メモリ基礎等の汎用または移入ライブラリを置く。

縦横の切り替えを共通ユニット内の大きな`case`文で処理しない。共通の表示契約を各タイプが別々に実装し、
Filterは選択されたタイプへ処理を委譲する。新しい表示タイプを追加する場合も専用フォルダーを作り、
既存の縦表示、横表示へ処理を追記しない。

テストも共通処理、縦表示、横表示で分離する。共通テストは表示タイプのユニットへ依存させず、
各表示タイプの座標と描画結果はそれぞれの専用テストで検証する。

## パラメータ命名方針

旧パラメータ名との互換性は前提にせず、初期実装時に名称を作り直す。共通項目には縦、横、上、下、
左、右等の特定レイアウトを前提とする語を使わず、縦表示と横表示のどちらでも意味が変わらない名称にする。
公開後は設定取得、書き戻し、既存プロジェクトとの互換性へ影響するため、確定名を安易に変更しない。

共通項目の名称候補は次のとおり。

| 名称候補 | 意味 |
| --- | --- |
| `表示タイプ` | 縦、横、将来追加する表示方式を選択する |
| `表示時間` | 発音位置から時間方向の表示端までに含める秒数 |
| `発音位置` | 時間方向の表示範囲内でノートが発音する位置 |
| `ずらし` | 音楽データとタイムラインの同期時刻差 |
| `表示音階数` | 基準表示音域へ含める連続MIDIノート数 |
| `中央ノート` | 基準表示音域の中央に置くMIDIノート番号 |
| `音域追従` | 基準音域から実効音域を動かす方式 |
| `ノート太さ` | 音階方向に対するノート矩形の太さ |
| `鍵盤の長さ` | 鍵盤が伸びる方向の長さ。縦横表示で同じ値を使う |
| `鍵盤の太さ` | 1鍵が音階方向に占める太さ。縦横表示で同じ値を使う |
| `レーン表示` | 白鍵・黒鍵に対応する背景レーンを表示するか |
| `拍線表示` | 拍線と小節線を表示するか |
| `1小節の拍数` | 小節線として強調する拍の間隔 |
| `ノート配色` | 単色、バリエーション1、将来追加する配色方式を選択する |
| `ノート単色` | 単色配色で全ノートへ適用する色 |
| `トラック` | 表示対象トラックの選択または絞り込み |

方向を説明する必要がある場合は、画面上の縦横ではなく`時間方向`と`音階方向`を優先する。
縦表示だけ、または横表示だけに存在する設定は共通項目へ混在させず、各表示タイプの専用設定として定義する。
GUIで表示タイプ別の項目を切り替えられない場合も、内部の設定構造とユニットは完全に分離しておく。

## Filter GUI設定

方向非依存の内部設定を次のFilter項目へ接続する。項目名はAviUtl2プロジェクト内の設定識別にも
使用されるため、公開後は互換性を確認せず変更しない。

| 項目名 | 初期値 | 範囲／選択肢 | 内部設定 |
| --- | ---: | --- | --- |
| `音楽ファイル` | 空 | 対応音楽形式 | 解析対象ファイル |
| `表示タイプ` | 縦 | 縦／横 | 表示実装の共通選択入口 |
| `サイズ初期値` | 小 | 小／中／大 | 一括適用するサイズプリセット |
| `サイズ初期値を適用` | - | ボタン | 選択したサイズを現在のオブジェクトへ反映 |
| `表示時間 (秒)` | 4.0 | 0.1～60.0 | `DisplayTime` |
| `発音位置 (%)` | 80 | 0～100 | `StrikePosition`へ0.0～1.0で格納 |
| `ずらし (秒)` | 0.0 | -60.0～60.0 | `TimeShift` |
| `表示音階数` | 128 | 1～128 | `VisibleNoteCount` |
| `中央ノート` | 64 | 0～127 | `CenterNote` |
| `音域追従` | 追従しない | 現在は追従しないのみ | `PitchFollowMode` |
| `鍵盤の長さ` | 60 | 0～1000 | `KeyLength` |
| `鍵盤の太さ` | 20 | 1～200 | `KeyThickness` |
| `ノート太さ (%)` | 80 | 5～100 | `NoteThickness`へ0.05～1.0で格納 |
| `レーン表示` | 表示 | 非表示／表示 | `ShowLanes` |
| `拍線表示` | 表示 | 非表示／表示 | `ShowBeatLines` |
| `1小節の拍数` | 4 | 1～32 | `BeatsPerMeasure` |
| `白鍵色` | RGB(242,242,242) | 色 | `Palette.WhiteKey` |
| `黒鍵色` | RGB(24,24,24) | 色 | `Palette.BlackKey` |
| `白鍵レーン色` | RGB(238,238,242) | 色 | `Palette.WhiteLane` |
| `黒鍵レーン色` | RGB(110,110,120) | 色 | `Palette.BlackLane` |
| `拍線色` | RGB(255,255,255) | 色 | `Palette.BeatLine` |
| `小節線色` | RGB(255,190,80) | 色 | `Palette.MeasureLine` |
| `発音線色` | RGB(255,255,255) | 色 | `Palette.StrikeLine` |
| `ノート配色` | 単色 | 単色／バリエーション1 | `TrackColorMode` |
| `ノート単色` | RGB(80,210,255) | 色 | `SingleTrackColor` |

ノート配色は単色を基準とする。`バリエーション1`はトラック番号を既存の6色パレットへ循環して
割り当てる。将来の配色は`TPianoRollTrackColorMode`と共通Colorユニットの色解決処理へ
`バリエーション2`以降として追加し、各表示タイプへ配色分岐を重複させない。

サイズ初期値は多数の寸法項目を個別に変更せず、次の基準値を一度に設定する。

| サイズ | 鍵盤の長さ | 鍵盤の太さ | ノート太さ |
| --- | ---: | ---: | ---: |
| 小 | 60px | 20px | 80% |
| 中 | 90px | 30px | 80% |
| 大 | 120px | 40px | 80% |

小は従来の初期値、中は小の1.5倍、大は小の2倍とする。拍線、小節線、発音線、鍵盤境界線は
追加のGUI項目を設けず、鍵盤の太さ20pxを基準として同じ倍率で線幅を拡大する。
選択リストを変更しただけでは個別値を上書きせず、`サイズ初期値を適用`ボタンを押したときだけ
AviUtl2の選択中オブジェクトへ`鍵盤の長さ・鍵盤の太さ・ノート太さ`をまとめて設定する。

各映像コールバックでAviUtl2が保持する項目値をローカルの`TPianoRollDisplaySettings`へコピーし、
範囲外値を防御的に補正してから表示実装へ渡す。複数オブジェクトの並列描画間で可変設定レコードを
共有しない。

## 共通カラーパレット

RGBA色型と既定色は`Source\Common\Color\SYNC_PianoRoll_Colors.pas`へ集約する。
RGBAキャンバス、表示設定、縦表示はこの共通色型を使用し、表示タイプ内へRGB定数を直接記述しない。

GUIへ公開する色は白鍵、黒鍵、白鍵／黒鍵レーン、拍線、小節線、発音線の7項目とする。
鍵盤境界色とトラック番号別の6色も共通パレットで管理するが、項目数を抑えるため現時点ではGUIへ
公開しない。色項目はAviUtl2 SDKの`B,G,R,X`配置で保持するが、`X`は予約領域であるため
描画アルファには使わない。GUIからRGBだけを取得し、レーンや線の透明度は共通パレットの既定値を維持する。

## 音楽ファイル解析とキャッシュ

Filterの`音楽ファイル`項目から元音楽ファイルを直接参照する。対応候補はSongReaderが扱う
`.mid`、`.midi`、`.ust`、`.vsq`、`.vsqx`、`.musicxml`、`.mxl`、`.xml`、`.mscx`、`.mscz`。
解析入口には内部保存形式用の`TSongData.LoadFromFile`ではなく、元音楽ファイル用の
`TSongData.LoadFromMusicFile`を使用する。

解析後は`TSongData`の可変オブジェクトをそのまま公開せず、ノート、拍、トラック、曲長を
`IPianoRollMusicData`の読み取り専用スナップショットへコピーする。ノートは開始秒、終了秒、
MIDIノート番号、ベロシティ、トラック番号、歌詞を保持する。拍は秒位置、番号、テンポを保持し、
トラックは番号と名称を保持する。

キャッシュキーには正規化した絶対パスを使用し、更新日時またはファイルサイズが変わった場合だけ
再解析する。同じファイルを指定した複数Filterは同じスナップショットを参照する。
キャッシュ置換後も既存の参照が解放されるまでスナップショットを保持できるよう、インターフェースで
参照カウントする。キャッシュ検索、生成、置換は排他して行う。

ファイル不存在、未知形式、破損、解析失敗、ファイルI/O例外は無効として扱い、画面表示や例外送出を
行わず映像を無加工で通す。無効な既存ファイルも更新日時とサイズが変わるまで再解析しない。

## 最小ピアノロール描画

現在は旧VCL描画系を移植せず、SDKの`SetImageData`へ透明RGBAバッファを直接渡す。
出力サイズはFilter対象の`Object_.Width`と`Object_.Height`を使用するため、仮想素材名で必要な寸法を
指定する。例えば1280×720、600秒、30fpsの描画ベースは次の名前にする。

```text
1280_720_600_30_1.syncpianoroll
```

縦表示では実効音域の低音を左、高音を右へ配置する。
発音位置は時間方向の80%とし、白い水平線で表示する。発音位置から時間方向の表示端までを
`表示時間`（初期値4秒）として座標へ変換し、未来のノートが上から発音位置へ流れる。
終了秒が未確定のノートは0.2秒として扱う。
現在時刻を過ぎて終了したノートは描画しない。ノート色は既定でGUI指定の単色を使用し、
バリエーション1ではトラック番号ごとの6色を使用する。

横表示では実効音域の高音を上、低音を下へ配置する。鍵盤は発音位置の左側へ伸ばし、
未来のノートは発音位置から右へ流す。共通の`発音位置`は未来側に確保する時間方向の割合として扱うため、
初期値80%では発音線を画面左から20%の位置へ垂直に描画する。縦表示の座標処理は呼び出さず、
横表示ユニットが同じ共通設定、音楽データ、鍵盤判定、配色、RGBAキャンバスを独立して利用する。

鍵盤とノートの音階方向位置は、半音を単純に等分せず、白鍵1個を1.0とする共通音階座標で計算する。
`鍵盤の太さ`（初期値20px）は白鍵1個の音階方向サイズ、`鍵盤の長さ`（初期値60px）は
発音位置から時間方向へ伸びる白鍵の長さとする。黒鍵は白鍵間の中心へ太さ62%、長さ62%で重ねる。
表示音域は固定ピクセルの`鍵盤の太さ`を維持し、`表示音階数`と`中央ノート`から決定する。
表示音階数は白鍵数ではなく、黒鍵を含む連続MIDIノート数とする。奇数個では中央ノートを範囲中央へ置き、
偶数個では中央ノートを上側の中央として扱う。MIDI範囲0～127の端へ達した場合は範囲全体を内側へ移し、
指定音階数を可能な限り維持する。

ユーザー設定から求める範囲を`基準表示音域`、各フレームの座標計算に使用する範囲を`実効表示音域`として
概念上分離する。現在は`追従しない`だけを実装するため両者は同じである。将来は音域幅を変えずに実効音域を
移動する`常に追従`と`はみ出したとき追従`を追加できるよう、`TPianoRollPitchFollowMode`を共通設定へ持つ。

発音位置より未来側には、白鍵レーンと黒鍵レーンを半透明で描画する。拍データはノートと同じ
時間座標へ変換し、通常拍を細い白線、小節先頭を太い橙線で描画する。現在のSongReaderが持つ拍データは
四分音符の通算インデックスで拍子記号を保持しないため、初期状態では4拍ごとを小節先頭として扱う。
将来拍子情報を解析データへ追加した場合は、固定値ではなく時刻ごとの拍子から小節を判定する。

描画バッファは`Object_.ID + Object_.EffectID`ごとのレンダーコンテキストへ保持する。
同じ出力サイズでは毎フレーム再利用し、寸法が変わった場合だけ再確保する。同じコンテキストの描画は
個別に排他し、異なるオブジェクトの描画を不要に直列化しない。Filter終了時に全バッファを解放する。

内部設定は`表示時間`、`発音位置`、`ずらし`、`表示音階数`、`中央ノート`、`音域追従`、
`鍵盤の長さ`、`鍵盤の太さ`、`ノート太さ`、`ノート配色`、`ノート単色`という
方向非依存の意味で保持する。配色判定は共通Colorユニットへ集約し、各表示タイプから再利用する。

## ビルド

Delphi 37.0、Win64のみを対象にし、DebugとReleaseを保つ。
出力先は `C:\ProgramData\aviutl2\Plugin\SYNC_PianoRoll` とする。
DebugではDLLとRSMを残し、Releaseではプラグイン拡張子へコピー後にDLLとRSMを削除する。

Input Debug Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\SYNC_PianoRoll\SYNC_PianoRoll_Input.dproj"" /t:Build /p:Config=Debug /p:Platform=Win64"
```

Filter Debug Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\SYNC_PianoRoll\SYNC_PianoRoll_Filter.dproj"" /t:Build /p:Config=Debug /p:Platform=Win64"
```

Input Release Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\SYNC_PianoRoll\SYNC_PianoRoll_Input.dproj"" /t:Build /p:Config=Release /p:Platform=Win64"
```

Filter Release Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\SYNC_PianoRoll\SYNC_PianoRoll_Filter.dproj"" /t:Build /p:Config=Release /p:Platform=Win64"
```

## 継続作業ルール

- コードを追加または変更するときは、実装だけで完了とせず、コメントルールに従ったコメントの追加・更新も同時に行う。
- 処理内容を変更した場合は、既存コメントが実装と一致しているか確認し、古くなったコメントを残さない。

## 次の実装候補

1. 単体Filter方式とInput＋Filter方式のどちらを正式構成にするか、標準変形の必要性を含めて決定する。
2. 表示音階数と中央ノートの極端な設定値を縦／横表示の実機上で確認する。
3. 音域追従の対象ノート、追従タイミング、移動規則を決め、`常に追従`と
   `はみ出したとき追従`を実装する。

## 作業ログ

- 2026-07-24: `SYNC_Motion`を参考に最小プロジェクトを作成した。Inputは透明映像を返し、Filterは映像を無加工で通す。
- 2026-07-24: Input／FilterのDebug・Release Win64をビルドし、4構成すべて警告0、エラー0を確認した。
- 2026-07-24: Release成果物を専用フォルダーへ配置し、Inputの`GetInputPluginTable`とFilterの
  `InitializePlugin`、`UninitializePlugin`、`GetFilterPluginTable`をロードできることを確認した。
- 2026-07-24: Git環境を`SYNC_Motion`と比較した。`main`と`origin/main`、リモートURL、利用者設定、
  追跡対象、除外規則は共通方針に一致している。管理対象テキストの作業ツリー改行をCRLFへ統一した。
- 2026-07-24: 旧`Syncroh2`のピアノロール用プロジェクト、フィルター実装、音楽拡張実装の参照先を記録した。
- 2026-07-24: 旧INI参照方式を移植せず、元音楽ファイルをFilterから直接参照して解析する方針を記録した。
- 2026-07-24: InputからFilterへ絶対フレームを渡す名前付き共有メモリと、Object ID＋Effect ID別の
  コンテキスト管理を追加した。共有更新が停止した場合はFilterローカルフレーム差分で補間する。
- 2026-07-24: 共有メモリ、2オブジェクト、同一Object ID上の別Effect、更新停止時の補間、
  新しい共有値による再基準化を回帰テストし、PASSを確認した。Debug／Releaseの4構成は警告0、エラー0。
- 2026-07-24: `SYNC_Motion`のSongReader一式を導入し、Filterへ`音楽ファイル`項目と
  読み取り専用スナップショットキャッシュを追加した。旧INIは介さず元音楽ファイルを直接解析する。
- 2026-07-24: 最小MIDIからノート番号60、ベロシティ100、開始0秒、終了0.5秒を取得し、
  同一ファイルでのキャッシュ再利用とファイルサイズ変更後の再解析を確認した。
  Filter登録表のロードテストもPASSし、Debug／Releaseの4構成は警告0、エラー0。
- 2026-07-24: 透明RGBAバッファへ縦方向のノート矩形と発音位置線を描く最小レンダラーを追加した。
  Object ID＋Effect ID別に描画バッファを再利用し、出力寸法が変わる場合だけ再確保する。
- 2026-07-24: 640×360と320×180のRGBA出力、ノート矩形描画、サイズ変更を回帰テストし、
  PASSを確認した。Debug／Releaseの4構成は警告0、エラー0。
- 2026-07-24: 旧ピアノロールをそのまま移植せず、新しい構造で作り直し、解析、判定、配色、
  座標計算等の独立した処理だけを選別して再利用する方針を確定した。
- 2026-07-24: 共通処理、プラグイン入口、縦表示、横表示、外部ライブラリをフォルダーで分離し、
  表示タイプ同士を完全に独立させる目標構成と、縦横に依存しないパラメータ命名方針を記録した。
- 2026-07-24: Input／Filter入口、共有フレームとコンテキスト、音楽データ、表示契約、RGBA描画、
  縦表示を`Source\Plugin`、`Source\Common`、`Source\Display\Vertical`へ実際に分離した。
  共通レンダラーから縦表示の座標計算を除き、`IPianoRollDisplay`を通じて表示実装へ委譲する構成にした。
- 2026-07-24: 新構成で共有フレーム／コンテキスト、音楽解析、縦表示RGBA描画、Filter DLLロードの
  4テストがPASSした。Input／FilterのDebug・Release Win64も4構成すべてビルド成功した。
- 2026-07-24: 旧実装から12音の白鍵／黒鍵規則だけを抽出し、
  `Source\Common\Data\SYNC_PianoRoll_PianoKeys.pas`へ方向非依存の判定と音階位置計算を追加した。
- 2026-07-24: 縦表示へ白鍵、黒鍵、境界線を追加し、ノートも同じ白鍵単位の音階座標へ揃えた。
  `鍵盤の長さ`と`鍵盤の太さ`を縦表示の時間方向と音階方向へ割り当てた。
- 2026-07-24: 白鍵／黒鍵判定、共有フレーム／コンテキスト、音楽解析、RGBA鍵盤・ノート描画、
  Filter DLLロードの5テストがPASSした。Input／FilterのDebug・Release Win64も全4構成で
  ビルド成功し、Releaseの`.auf2`をロードできることを確認した。
- 2026-07-24: 鍵盤と同じ音階座標で半透明の白鍵・黒鍵レーンを追加した。解析済み拍データを
  ノートと同じ時間座標へ変換し、通常拍線と4拍ごとの小節線を描き分けた。
- 2026-07-24: `レーン表示`、`拍線表示`、`1小節の拍数`を方向非依存の内部設定へ追加した。
- 2026-07-24: RGBA描画テストへ白鍵レーン、黒鍵レーン、通常拍線、小節線の画素検証を追加した。
  全5テストがPASSし、Input／FilterのDebug・Release Win64は全4構成でビルド成功、
  Debugフィルターは警告0・エラー0、Releaseの`.auf2`もロード成功を確認した。
- 2026-07-24: `表示時間 (秒)`、`発音位置 (%)`、`ずらし (秒)`、音域、鍵盤サイズ、
  ノート太さ、レーン／拍線表示、1小節の拍数をFilter GUIへ接続した。GUI値はコールバックごとの
  ローカル設定へ安全に変換し、表示実装へ渡す構成とした。
- 2026-07-24: Filter登録表テストを13項目へ拡張し、項目順、型、名称、初期値、
  項目配列と選択肢配列のnil終端を確認した。
- 2026-07-24: GUI接続後も全5テストがPASSした。Input／FilterのDebug・Release Win64は
  全4構成でビルド成功し、Debugフィルターは警告0・エラー0、Releaseの`.auf2`もロード成功を確認した。
- 2026-07-24: RGBA色型、鍵盤、レーン、拍線、小節線、発音線、トラック6色を
  `Source\Common\Color\SYNC_PianoRoll_Colors.pas`の共通パレットへ分離した。
- 2026-07-24: 白鍵色、黒鍵色、白鍵／黒鍵レーン色、拍線色、小節線色、発音線色の7項目を
  Filter GUIへ追加した。登録表テストでSDKのBGRX配置と予約領域の初期値を確認し、
  描画テストで任意RGBA色が白鍵へ反映されることを確認した。
- 2026-07-24: 共通パレットとGUI色接続後も全5テストがPASSした。Input／Filterの
  Debug・Release Win64は全4構成でビルド成功し、Debugフィルターは警告0・エラー0、
  Releaseの`.auf2`もロード成功を確認した。
- 2026-07-27: ノート配色を共通Colorユニットへ集約し、GUI指定色を全ノートへ適用する`単色`と
  固定6色を循環する`バリエーション1`を選択できるようにした。単色を既定とし、既存20項目の順序を維持して
  `ノート配色`と`ノート単色`をFilter項目の末尾へ追加した。
- 2026-07-27: 単色／バリエーション1の画素検証とFilter登録表検証を追加し、全5テストがPASSした。
  Input／FilterのDebug・Release Win64は全4構成でビルド成功し、警告0・エラー0、
  Releaseの`.auf2`もロード成功を確認した。
- 2026-07-27: `表示タイプ`をFilter項目の末尾へ追加し、現在は`縦`だけを選択肢として登録した。
  選択値から専用表示実装を解決する共通入口を追加し、横表示を後から独立して接続できる構成にした。
- 2026-07-27: 表示タイプ入口追加後も全5テストがPASSし、実DLLの登録表ロードも成功した。
  Input／FilterのDebug・Release Win64コンパイルは警告0・エラー0。AviUtl2が配置済み`.auf2`を
  使用中のため、今回のFilter成果物を`.auf2`へ上書きするビルド後コピーだけは未実施。
- 2026-07-27: `Source\Display\Horizontal`へ横表示を独立実装し、鍵盤を発音位置の左、
  未来ノートを右、高音を上、低音を下へ配置した。白鍵／黒鍵、レーン、拍線／小節線、発音線、
  単色／配色バリエーションは共通設定を通じて縦表示と同じ値を利用する。
- 2026-07-27: `表示タイプ`の選択肢へ`横`を追加し、横表示専用の画素・座標テストを新設した。
  全6テストがPASSし、FilterのDebug／Release相当DLLは警告0・エラー0でコンパイル成功、
  両DLLの登録表ロードも成功した。配置済み`.auf2`はAviUtl2が使用中のため上書きしていない。
- 2026-07-27: AviUtl2実機上で横表示を確認した。鍵盤が発音位置の左、未来ノートが右、
  高音が上、低音が下へ配置され、レーン、拍線、小節線、発音線、単色ノートが描画されることを確認した。
- 2026-07-27: Filter登録から`FILTER_FLAG_FILTER`を外し、タイムラインへ直接配置する
  単体映像フィルターへ変更した。Inputの共有フレーム受信を動作条件から外し、単体フィルターへ渡される
  `Object_.Time`を音楽同期時刻、`Object_.Width`と`Object_.Height`を画面全体の描画座標として使う。
  旧Input、共有メモリ、コンテキスト処理は互換確認用として残すが、現在のFilterからは参照しない。
- 2026-07-27: 単体時刻テストを追加し、縦／横描画テストでコールバックへ渡した縦横寸法が
  出力キャンバスへ反映されることを確認した。全7テストがPASSし、FilterのDebug／Release相当DLLは
  エラー0でビルド成功、両DLLの登録表が単体映像フィルターのフラグを持つことをロード検証した。
  AviUtl2終了後にRelease版を配置済み`.auf2`へ反映し、配置先ファイルの登録表ロードもPASSした。
- 2026-07-27: 音階方向は固定ピクセル描画を維持し、基準表示音域の指定を
  `最低音・最高音・自動音域`から`表示音階数・中央ノート・音域追従`へ変更した。
  表示音階数は連続MIDIノート数とし、中央からの範囲計算と0／127端での範囲移動を共通処理へ集約した。
  基準表示音域と将来動かす実効表示音域を分け、追従方式の列挙型へ`追従しない`、`常に追従`、
  `はみ出したとき追従`を定義した。GUIと動作は現段階では`追従しない`だけを公開する。
- 2026-07-27: 中央ノート方式の通常範囲、全音域、0／127端補正を回帰テストへ追加した。
  縦／横描画とFilter登録表を含む全7テストがPASSし、FilterのDebug／Release相当DLLは
  エラー0でビルド成功した。Release版を配置済み`.auf2`へ反映し、配置先ファイルのロードもPASSした。
- 2026-07-27: 実Filter DLLの登録表で`表示音階数・中央ノート`を書き換えて映像コールバックを呼ぶ
  統合テストを追加した。128音／中央64と1音／中央60で不透明画素数が変化し、GUI項目値から
  描画設定までの経路が機能することを確認した。現在の固定ピクセル方式では表示音階数は拡大率ではなく、
  中央へ配置する鍵盤とノートの対象範囲だけを変更する。
- 2026-07-27: `Aul2AudioFilter`のプリセット選択と適用ボタンを参考に、サイズ初期値の
  `小・中・大`と一括適用ボタンをFilter項目末尾へ追加した。小は従来値、中は1.5倍、大は2倍とし、
  鍵盤寸法とノート太さを選択中オブジェクトへまとめて設定する。拍線、小節線、発音線、鍵盤境界線も
  鍵盤の太さに連動して拡大する。
- 2026-07-27: サイズプリセット選択肢とボタン登録、ボタンによる大サイズ3項目の一括更新、
  大サイズでの発音線拡大を回帰テストへ追加した。全8テストがPASSし、FilterのDebug／Release相当DLLは
  エラー0でビルド成功した。Release版を配置済み`.auf2`へ反映し、配置先で登録表ロードと
  プリセットボタン適用の統合テストがPASSした。
- 2026-07-27: 単体Filterはシーン全体へ直接描画するため、ベース画像オブジェクトに備わる
  位置移動、回転、拡大率、透明度等の標準変形を通常どおり利用できない問題を実機上で確認した。
  標準変形が必要な場合は同じフォルダーのInputをベースとしてFilterを追加する構成が必要になる。
  問題点と構成上の選択肢だけを記録し、この時点ではコードを変更しない。
