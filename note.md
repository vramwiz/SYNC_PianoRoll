# SYNC_PianoRoll 作業ノート

## 目的

MIDI等の音楽データを読み、AviUtl2上で鍵盤とノートを音楽へ同期して描画する。
Filterの単体配置では画面全体へ描画し、Input＋Filter配置では標準変形を利用できるようにする。

## 現在の最小構成

- `SYNC_PianoRoll_Filter.dpr/.dproj`: 音楽ファイルを解析し、単体配置またはInputベース上で選択した表示実装へ描画を委譲する。成果物は `.auf2`。
- `SYNC_PianoRoll_Input.dpr/.dproj`: 標準変形を使う構成の透明ベース画像を提供し、単体配置との互換確認用として同じフォルダーへ置く。
- `Source\Plugin\Filter\SYNC_PianoRoll_FilterPlugin.pas`: Filter登録、設定取得、表示実装の選択を担当する。
- `Source\Plugin\Input\SYNC_PianoRoll_InputPlugin.pas`: ファイル名からベース画像の寸法、時間、フレームレートを生成する。
- `Source\Common\Timeline\SYNC_PianoRoll_Time.pas`: 単体FilterとInput＋Filterの配置方式に応じた同期時刻を取得する。
- `Source\Common\Timeline\SYNC_PianoRoll_FrameShared.pas`: Inputが受け取った開始時間込みのフレームをFilterへ共有する。
- `Source\Common\Timeline\SYNC_PianoRoll_ContextManager.pas`: Inputフレームとローカルフレームをオブジェクト別に対応付けて補間する。
- `Source\Common\Data\SYNC_PianoRoll_MusicData.pas`: 元音楽ファイルの解析結果を描画用の読み取り専用スナップショットとして共有する。
- `Source\Common\Color\SYNC_PianoRoll_Colors.pas`: RGBA色型、描画共通パレット、単色と配色バリエーションを定義する。
- `Source\Common\Layout\SYNC_PianoRoll_DisplayTypes.pas`: 縦横に依存しない表示設定と表示実装の共通契約を定義する。
- `Source\Common\Layout\SYNC_PianoRoll_PitchFollow.pas`: 発音中または直近未来のノートから実効表示音域を決定する。
- `Source\Common\Render\SYNC_PianoRoll_RGBA.pas`: 表示タイプに依存しないRGBAキャンバスと矩形描画を提供する。
- `Source\Common\Render\SYNC_PianoRoll_Renderer.pas`: Filter対象の縦横寸法で描画バッファを管理し、選択された表示実装を呼び出す。
- `Source\Display\Vertical\SYNC_PianoRoll_VerticalDisplay.pas`: 時間方向を下向き、音階方向を左から右へ割り当てる縦表示を担当する。
- `Source\Display\Horizontal\SYNC_PianoRoll_HorizontalDisplay.pas`: 時間方向を右向き、音階方向を上から下へ割り当てる横表示を担当する。
- `Source\Display\Vertical3D\SYNC_PianoRoll_Vertical3DDisplay.pas`: 音階X、時間Y、厚みZの縦3D頂点を生成する。
- `Source\Display\Horizontal3D\SYNC_PianoRoll_Horizontal3DDisplay.pas`: 時間X、音階Y、厚みZの横3D頂点を生成する。
- `Source\Lib\AviUtl2InputTypes.pas`: 入力プラグインSDKの最小Delphi ABI定義。
- `Source\Lib\AviUtl2FilterTypes.pas`: フィルタープラグインSDKの最小Delphi ABI定義。
- `Source\Lib\SharedMemoryBase.pas`: 名前付き共有メモリの最小基礎処理。
- `Source\Lib\SongReader`: `SYNC_Motion`から導入したMIDI、UST、VSQX、MusicXML、MuseScore解析処理。
- `Tests\SYNC_PianoRoll_FrameContextTests.dpr`: 共有メモリとオブジェクト別フレーム補間の回帰テスト。
- `Tests\SYNC_PianoRoll_MusicDataTests.dpr`: 最小MIDIの直接解析とキャッシュ更新の回帰テスト。
- `Tests\SYNC_PianoRoll_PianoKeyTests.dpr`: 白鍵・黒鍵判定、白鍵単位の音階位置、白黒共通のノート幅を検証する。
- `Tests\SYNC_PianoRoll_ColorTests.dpr`: 既存配色と旧実装から移植した13種類の共通色計算を検証する。
- `Tests\SYNC_PianoRoll_PitchFollowTests.dpr`: 3種類の音域追従、和音、未来ノート、MIDI範囲端を検証する。
- `Tests\SYNC_PianoRoll_PluginLoadTests.dpr`: Filter登録項目、初期値、選択肢配列のロードテスト。
- `Tests\SYNC_PianoRoll_FilterSettingsTests.dpr`: 実Filter DLLの音域設定とサイズプリセット適用を検証する。
- `Tests\SYNC_PianoRoll_RenderTests.dpr`: 縦表示のRGBA出力、配色、寸法変更、線幅連動、打鍵発光を検証する描画テスト。
- `Tests\SYNC_PianoRoll_HorizontalRenderTests.dpr`: 横表示の座標、鍵盤、レーン、拍線、ノート、打鍵発光を検証する。
- `Tests\SYNC_PianoRoll_Perspective3DTests.dpr`: 縦横3Dの座標軸、押し出し、ハープ鍵盤、DrawPoly境界を検証する。
- `Tests\SYNC_PianoRoll_StandaloneTimeTests.dpr`: 単体Filterのローカル時刻とInput＋Filterの開始時間込み同期を検証する。

AviUtl2上の識別名は次の値で固定する。

```text
SYNC_ピアノロール_Input
SYNC_ピアノロール_Filter
```

入力用の仮想素材拡張子は `.syncpianoroll` とし、次のファイル名形式を解釈する。

```text
Width_Height_MaxSec_Fps_Scale.syncpianoroll
```

値を省略または解釈できない場合は、幅1、高さ1、3600秒、30fps、scale 1を使う。

## Filterの配置方式、座標と時刻

Filterは、タイムラインへ直接配置する単体映像フィルターとしても、`SYNC_PianoRoll_Input`で生成した
ベース画像オブジェクトへ追加するFilterとしても使用できる。

描画座標の幅と高さは、映像コールバックの`Object_.Width`と`Object_.Height`をそのまま使う。
単体フィルターではこの縦横値に画面全体の描画範囲が渡されるため、別の素材寸法や固定解像度を基準にしない。
Input＋Filter方式ではInputのファイル名から生成したベース画像の寸法が渡される。
縦表示と横表示は同じRGBAキャンバス寸法を受け取り、それぞれの時間方向と音階方向へ座標を割り当てる。

単体Filterでは`Object_.Time`を使い、Filterの配置先頭を音楽データの0秒とする。
Input＋Filter方式ではInputの`func_read_video`へ渡されたフレームを共有し、入力設定の`開始時間`を含む
時刻を音楽との同期に使う。Input再取得が省略された再描画では、Object ID＋Effect ID別に保持した
Inputフレームと`Object_.Frame`の対応から時刻を補間する。新規基準には直近1秒以内に更新された
共有値だけを採用し、過去のオブジェクトやプロジェクトの値を誤用しない。

単体Filterはシーン全体へ直接描画するため、ベース画像オブジェクトの標準変形を利用できない。
位置移動、回転、拡大率、透明度等が必要な場合はInput＋Filter方式を使う。
配布するエイリアスはInputとFilterを組み合わせた状態を生成し、標準変形を利用できることを実機確認済みである。

## 旧Syncroh2の参照実装

ピアノロール機能の仕様と実装を調査する際は、次の旧`Syncroh2`資産を参照する。

```text
D:\DelphiProg\test\Syncroh2\Syncroh2_Filter_PianoRoll.dpr
D:\DelphiProg\test\Syncroh2\Plugin_Filter\PianoRoll
D:\DelphiProg\test\Syncroh2\Plugin_Extension\Music\PianoRoll
```

- `Syncroh2_Filter_PianoRoll.dpr`: 旧ピアノロールフィルターのプロジェクト構成と使用ユニットを確認する。
- `Plugin_Filter\PianoRoll`: フィルター登録、設定項目、ピアノロール描画処理を確認する。
- `Plugin_Extension\Music\PianoRoll`: 音楽データからピアノロール表示へ変換する処理を確認する。
- 旧構成をそのままコピーせず、現在のInput／Filter構成に必要な処理と依存ユニットだけを選別して取り込む。
- 現在のAviUtl2 SDK ABI、安全方針、並列呼び出し、キャッシュ設計を優先し、旧実装との差異を確認して移植する。

## 旧実装からの重要な変更

旧`Syncroh2`では、事前に生成または保存されたINIファイルを介してピアノロール用データを参照していた。
`SYNC_PianoRoll`ではINIファイルを入力元とせず、MIDI等の元音楽ファイルをFilterから直接参照して解析する。

- 利用者は中間INIファイルを用意せず、Filterの設定項目で元音楽ファイルを指定する。
- 対応形式はSongReaderの採用範囲を決めた後に確定し、Filterのファイル選択候補へ列挙する。
- 解析結果は元ファイルの正規化した絶対パス、更新日時、ファイルサイズ等を基準にキャッシュする。
- 同じ音楽ファイルを複数オブジェクトが参照する場合は、解析結果を読み取り専用で共有する。
- 元ファイルの不存在、未知形式、破損、解析失敗時は例外をAviUtl2へ漏らさず、安全に無描画または無加工で通す。
- 旧INIの構造や保存方式との互換性は前提にせず、必要な表示設定だけを新しい構成で定義する。

## 作り直しと再利用の方針

旧`Syncroh2`のピアノロール全体をそのまま移植しない。新しい構造で作り直し、単独で責務を説明でき、
現在の設計へ無理なく適合できる処理だけを選別して再利用する。旧実装は部品の参照元、動作見本、
仕様確認用として残し、新版のクラス構造やデータ経路の基礎にはしない。

再利用候補は次のとおり。

- SongReaderによる元音楽ファイルの解析。
- MIDIキー、音階、白鍵、黒鍵等の判定。
- トラック別、音階別、階名別の配色計算。
- ノート、鍵盤、拍線等の座標計算式。
- 実際の利用で必要性が確認された初期値、範囲、描画順序。

次の処理は新しい設計で作る。

- Input／Filter間の共有メモリと時刻同期。
- 読み取り専用音楽データ、キャッシュ、並列呼び出し対策。
- 表示タイプを選択し、専用処理へ委譲する仕組み。
- レイアウト計算とRGBA描画の分離。
- AviUtl2の設定項目と安全なコールバック境界。
- 表示設定の保持方法と、将来必要になる永続データの形式。

旧INIを中心としたデータ経路、RTTI／INI保存クラス、VCLフォーム、`TBitmap`依存の描画、
旧共有メモリと旧コンテキスト管理の実装そのもの、複数の責務が混在した巨大ユニットは移植しない。
Input／Filter間の同期は開始時間を渡す最小共有状態と、オブジェクト別の小さな時刻基準として作り直す。

## ソースフォルダーの分離方針

旧実装では共通処理と表示タイプ固有処理の置き場所が分かりにくくなったため、今後は責務と再利用範囲を
フォルダーで明示する。共通処理から縦表示や横表示の実装を参照せず、各表示タイプから共通処理を利用する
一方向の依存関係とする。表示タイプ同士は互いを参照しない。

次の構成を基本とする。Input／Filter入口、データ、時刻同期、表示契約、RGBA描画、縦表示は
2026-07-24にこの構成へ移動済み。`Color`も共通パレットの実装時に追加済み。
`Horizontal`は2026-07-27に独立した横表示の実装とともに追加した。

```text
Source
├─ Plugin
│  ├─ Input
│  └─ Filter
├─ Common
│  ├─ Data
│  ├─ Timeline
│  ├─ Layout
│  ├─ Render
│  └─ Color
├─ Display
│  ├─ Vertical
│  ├─ Horizontal
│  ├─ Vertical3D
│  └─ Horizontal3D
└─ Lib
   ├─ SongReader
   └─ AviUtl2 SDK／汎用基礎処理
```

- `Source\Plugin`: AviUtl2への登録、コールバック、設定値の受け渡しだけを担当する。
- `Source\Common\Data`: 読み取り専用音楽データ、ノート、拍、トラック、キャッシュを置く。
- `Source\Common\Timeline`: 共有フレーム、オブジェクト別コンテキスト、時刻変換を置く。
- `Source\Common\Layout`: 表示タイプが実装する共通契約、座標、矩形等の値型を置く。
- `Source\Common\Render`: RGBAバッファ、線、矩形、クリップ等の表示タイプに依存しない描画処理を置く。
- `Source\Common\Color`: トラック、音階、階名等の共通配色処理を置く。
- `Source\Display\Vertical`: 縦表示だけのレイアウト、鍵盤、ノート、拍線、発音表示を置く。
- `Source\Display\Horizontal`: 横表示だけのレイアウト、鍵盤、ノート、拍線、発音表示を置く。
- `Source\Display\Vertical3D`: 縦3Dだけの頂点配置、鍵盤、ノート、押し出し形状を置く。
- `Source\Display\Horizontal3D`: 横3Dだけの頂点配置、鍵盤、ノート、押し出し形状を置く。
- `Source\Lib`: SongReader、AviUtl2 SDK定義、共有メモリ基礎等の汎用または移入ライブラリを置く。

縦横の切り替えを共通ユニット内の大きな`case`文で処理しない。共通の表示契約を各タイプが別々に実装し、
Filterは選択されたタイプへ処理を委譲する。新しい表示タイプを追加する場合も専用フォルダーを作り、
既存の縦表示、横表示へ処理を追記しない。

テストも共通処理、縦表示、横表示で分離する。共通テストは表示タイプのユニットへ依存させず、
各表示タイプの座標と描画結果はそれぞれの専用テストで検証する。

## パラメータ命名方針

旧パラメータ名との互換性は前提にせず、初期実装時に名称を作り直す。共通項目には縦、横、上、下、
左、右等の特定レイアウトを前提とする語を使わず、縦表示と横表示のどちらでも意味が変わらない名称にする。
公開後は設定取得、書き戻し、既存プロジェクトとの互換性へ影響するため、確定名を安易に変更しない。

共通項目の名称候補は次のとおり。

| 名称候補 | 意味 |
| --- | --- |
| `表示分類` | ピアノ、将来追加する別楽器・別構造を選択する |
| `描画方式` | 2Dまたは3Dを選択する |
| `表示タイプ` | 分類と描画方式の中でType1、Type2等を選択する |
| `方向` | 時間軸と音階軸を縦または横のどちらへ配置するか選択する |
| `鍵盤タイプ` | 標準ピアノまたはハープ向け7音鍵盤を選択する |
| `表示時間` | 発音位置から時間方向の表示端までに含める秒数 |
| `表示時間 3D` | 3D形状として生成する未来範囲の秒数 |
| `発音位置` | 時間方向の表示範囲内でノートが発音する位置 |
| `ずらし` | 音楽データとタイムラインの同期時刻差 |
| `表示音階数` | 基準表示音域へ含める連続MIDIノート数 |
| `中央ノート` | 基準表示音域の中央に置くMIDIノート番号 |
| `音域追従` | 基準音域から実効音域を動かす方式 |
| `ノート太さ` | 音階方向に対するノート矩形の太さ |
| `鍵盤の長さ` | 鍵盤が伸びる方向の長さ。縦横表示で同じ値を使う |
| `鍵盤の太さ` | 1鍵が音階方向に占める太さ。縦横表示で同じ値を使う |
| `レーン表示` | 白鍵・黒鍵に対応する背景レーンを表示するか |
| `拍線表示` | 拍線と小節線を表示するか |
| `1小節の拍数` | 小節線として強調する拍の間隔 |
| `ノート配色` | 単色、バリエーション1、将来追加する配色方式を選択する |
| `ノート単色` | 単色配色で全ノートへ適用する色 |
| `トラック` | 表示対象トラックの選択または絞り込み |

方向を説明する必要がある場合は、画面上の縦横ではなく`時間方向`と`音階方向`を優先する。
縦表示だけ、または横表示だけに存在する設定は共通項目へ混在させず、各表示タイプの専用設定として定義する。
GUIで表示タイプ別の項目を切り替えられない場合も、内部の設定構造とユニットは完全に分離しておく。

### 表示スタイルと設定構成

長い合成名を1つのリストへ並べず、`表示分類`、`描画方式`、`表示タイプ`、`方向`の
4項目を独立して選択する。現在の選択肢は次のとおりとし、未実装の分類とTypeは追加しない。

| 設定 | 現在の選択肢 |
| --- | --- |
| `表示分類` | ピアノ |
| `描画方式` | 2D、3D |
| `表示タイプ` | Type1 |
| `方向` | 縦、横 |

`鍵盤タイプ`はこの4分類とは別に置き、`標準ピアノ`と`ハープ（7音）`を選択する。
現在実装済みの組み合わせは、ピアノ／2D／Type1とピアノ／3D／Type1の縦・横である。
3Dは方向ごとの専用ユニットへ分け、縦は音階X・時間Y、横は時間X・音階Yとして描画する。
`DrawPoly`を利用できない場合は、同じ方向の2D Type1へ安全にフォールバックする。

時間、音階、発音位置、鍵盤、レーン、ノート、拍線、配色等の設定項目と音楽データは、
すべてのスタイルから使い回す。共通Color、RGBAキャンバス、音階判定等の方向非依存部品も共有する。
一方、縦表示と横表示のレイアウトおよび描画処理は共通化せず、現在の独立実装を維持する。
座標軸を抽象化した共通描画層は設けず、各スタイルが専用処理として配置、座標計算、描画順序、
使用する要素の組み合わせを定義する。

将来の表示スタイルとして3D表示も検討対象に含める。ただしFilter内部からAviUtl2の細かな
オブジェクト座標系や3D変形へ干渉しない。位置、回転、拡大率、奥行き等の調整は、
Input＋Filter方式を含むAviUtl2側のオブジェクト設定で利用者が行うものとする。

3Dまたは立体的な表示スタイルは、次の方向を検討候補とする。

| 検討案 | 表示内容 |
| --- | --- |
| 円環／トンネル型 | 鍵盤を円環または螺旋状に配置し、時間方向を画面奥へ伸ばす。ノート、拍線、発音マーカーも奥行きに沿って収束させる |
| 透視平面型 | 鍵盤とノートレーンを立体空間内の平面として配置し、斜め視点から奥へ流れるノートを表示する |
| 中央発音型 | 画面中央の発音線または発音点へノートを集め、上下または左右へ対称に展開する。鍵盤を常設せず、発音マーカーを主表示にすることも検討する |

円環、遠近投影、奥行き方向のノート配置等、スタイルそのものを成立させるための投影計算は
各スタイル内部で行う。FilterがAviUtl2オブジェクトの座標やカメラ設定を操作することはせず、
スタイルが生成した2D映像または3D頂点に対する最終的な位置、回転、拡大率等は
利用者がAviUtl2側で調整する。

### 透視平面3Dの現行仕様

最初の3D表示は`Aul2AudioView`の3D描画を参考にし、AviUtl2の`DrawPoly`へ色付き四角形の
XYZ頂点を直接渡す`3D平面`として実装した。編集解像度を超える巨大な2D画像を生成してから
回転する方式は採らず、音階と時間の平面をオブジェクト空間へ構築する。
形状自体には初期傾斜を持たせず、初期状態で2Dと同じ正面向きになるX-Y平面とする。
縦は音階をX軸、時間をY軸、横は時間をX軸、音階をY軸へ割り当て、どちらも実形状の厚みを
Z軸へ割り当てる。最終的な位置、カメラ角度、回転、拡大率はAviUtl2側で調整する。

`表示時間`は2Dの画面端までの時間であると同時に、3Dでも1秒あたりの時間軸長を決める。
`表示時間 3D`は、その縮尺を変えずに3D頂点を何秒先まで生成するかを決める。
初期値は`30秒`、範囲は`0.1～600秒`とする。例えば`表示時間=4秒`、
`表示時間 3D=30秒`では、正面表示の密度は4秒基準のまま、カメラを傾けると
最大30秒先まで続く平面を表示できる。2D描画では`表示時間 3D`を使用しない。

初回の描画対象はレーン背景、ノート、拍線、小節線、発音線とする。標準ピアノ／ハープ、
音域追従、表示時間、ずらし、ノート配色等の共通設定は既存実装と共有する。鍵盤本体、
発音時の発光、立体ノートの厚みは、基本の奥行き描画を実機確認した後の段階で追加を検討する。
`DrawPoly`を利用できない環境では、描画不能にせず選択方向と同じ2D表示へフォールバックする。

実機で基本の奥行き描画を確認後、3D表示へ鍵盤本体と実形状の厚みを追加した。
`白鍵3D厚み`、`黒鍵3D厚み`、`ノート3D厚み`を独立した数値設定とし、いずれも初期値`0`、
範囲`0～500`とする。`0`では上面だけの平面、正の値では平面の法線方向へ押し出した上面と
左右および手前の3側面を描画する。奥側端面は上面へ重なって見える問題を避けるため生成しない。
側面を先に、上面を最後に描画する。白鍵と黒鍵を分けることで、同じ薄板、黒鍵だけを高くしたピアノ形状、
独立した抽象表現を選べるようにする。側面色は上面色から自動的に暗く生成し、色設定は増やさない。
ハープでは半音鍵を描かないため、`黒鍵3D厚み`は使用しない。

白鍵とハープ鍵は隙間なく連続するため、鍵ごとの個別ボックスにはしない。上面は同じ幅の鍵単位で
描いて境界を維持し、左右側面は鍵盤全体の最外周だけ、手前側面は連続した1面だけ生成する。
隣接鍵の共有境界へ内部側面を作らず、カメラ角度によって片側だけ灰色の隙間に見えることを防ぐ。
黒鍵は互いに離れているため、鍵ごとの個別押し出しを維持する。正面では白鍵を基準Yから下へ
伸ばし、黒鍵も同じ方向へ短く伸ばして白鍵のY範囲内へ重ねる。厚み0では白鍵と黒鍵の上面Zを
同じ位置に固定する。白鍵厚みは上面を動かさず背面Zへ、黒鍵厚みは白鍵上面を底として
手前Zへ伸ばす。これにより白鍵厚みを変更しても黒鍵との接点と正面位置を維持する。

既存の`ノート立体表示`は2D表示の縁取り、面取り、反射帯を切り替える設定であり、
`ノート3D厚み`はXYZ頂点を実際に押し出す設定として区別する。既存の
`ノート立体表示`の名称整理は設定互換性も考慮して別途検討する。

旧方式にあった`鍵盤の表示／非表示`と`ノートの表示／非表示`は、独立したユーザー設定としては
いったん採用しない。鍵盤やノートを使うかどうかは各スタイルの仕様へ含め、スタイル側で決定する。

利用者が細かなパラメーターを頻繁に調整しない前提とし、GUI項目は簡単設定と詳細設定に分ける。
上部には表示スタイルや鍵盤サイズの`小／中／大`等、通常利用で必要な少数の簡単設定を集中させる。
寸法、色、線、位置等の個別調整は詳細設定として下部へまとめる。簡単設定の変更によって詳細値を
どの時点で更新するかは、現在のサイズ適用ボタン方式も含めて実装前に決定する。

ノート矩形へ疑似的な立体感を加える表示を試作した。実際の3D座標へ変換するのではなく、
ノート色を基準に次の描画を重ねる。

- 外周へ暗い縁取りを付け、背景とノートの境界を明確にする。
- 光が当たる側の内縁を明るくし、反対側を暗くして浅い面取りに見せる。
- ノート表面の一部へ白またはノート色を明るくした半透明帯を重ね、つやを表現する。
- 小さすぎて面取りが潰れるノートは、自動的に従来の平面描画へ戻す。

Filter末尾へ`ノート立体表示`を追加し、`平面／立体`から選択する。初期値は`立体`とする。
縁の太さ、つやの幅、明暗量は現時点では固定し、公開する必要が生じた場合だけ詳細設定へ追加する。
設定の意味と配色計算は全スタイルで共有するが、縦横の描画処理は共通化せず、それぞれの専用実装で
時間方向と音階方向に合う縁・つやの向きを決定する。縦表示は左側、横表示は上側へ反射帯を置く。
発音時の鍵盤点灯と放射光を隠さないよう、ノートの後に放射光を描画する。

## Filter GUI設定

方向非依存の内部設定を次のFilter項目へ接続する。項目名はAviUtl2プロジェクト内の設定識別にも
使用されるため、公開後は互換性を確認せず変更しない。

| 項目名 | 初期値 | 範囲／選択肢 | 内部設定 |
| --- | ---: | --- | --- |
| `音楽ファイル` | 空 | 対応音楽形式 | 解析対象ファイル |
| `サイズ` | 中 | 中／大 | 一括適用するサイズプリセット |
| `サイズ適用` | - | ボタン | 選択したサイズを現在のオブジェクトへ反映 |
| `表示タイプ` | 縦 | 縦／横 | 表示実装の共通選択入口 |
| `鍵盤タイプ` | 標準ピアノ | 標準ピアノ／ハープ（7音） | `KeyboardType` |
| `表示時間 (秒)` | 4.0 | 0.1～60.0 | `DisplayTime` |
| `表示時間 3D (秒)` | 30.0 | 0.1～600.0 | `DisplayTime3D` |
| `発音位置 (%)` | 80 | 0～100 | `StrikePosition`へ0.0～1.0で格納 |
| `ずらし (秒)` | 0.0 | -60.0～60.0 | `TimeShift` |
| `表示音階数` | 64 | 1～128 | `VisibleNoteCount` |
| `中央ノート` | 64 | 0～127 | `CenterNote` |
| `音域追従` | 追従しない | 追従しない／常に追従／はみ出したとき追従 | `PitchFollowMode` |
| `鍵盤の長さ` | 120 | 0～1000 | `KeyLength` |
| `鍵盤の太さ` | 40 | 1～200 | `KeyThickness` |
| `ノート太さ (%)` | 80 | 5～100 | `NoteThickness`へ0.05～1.0で格納 |
| `ノート立体表示` | 立体 | 平面／立体 | `NoteDepthEnabled` |
| `レーン表示` | 表示 | 非表示／表示 | `ShowLanes` |
| `拍線表示` | 表示 | 非表示／表示 | `ShowBeatLines` |
| `1小節の拍数` | 4 | 1～32 | `BeatsPerMeasure` |
| `白鍵色` | RGB(242,242,242) | 色 | `Palette.WhiteKey` |
| `黒鍵色` | RGB(24,24,24) | 色 | `Palette.BlackKey` |
| `白鍵レーン色` | RGB(238,238,242) | 色 | `Palette.WhiteLane` |
| `黒鍵レーン色` | RGB(110,110,120) | 色 | `Palette.BlackLane` |
| `拍線色` | RGB(255,255,255) | 色 | `Palette.BeatLine` |
| `小節線色` | RGB(255,190,80) | 色 | `Palette.MeasureLine` |
| `発音線色` | RGB(255,255,255) | 色 | `Palette.StrikeLine` |
| `ノート配色` | 単色 | 単色／バリエーション1／旧13方式 | `TrackColorMode` |
| `ノート単色` | RGB(80,210,255) | 色 | `SingleTrackColor` |
| `グラデ色1` | RGB(255,0,0) | 色 | `GradientColor1`（高音側） |
| `グラデ色2` | RGB(0,0,255) | 色 | `GradientColor2`（低音側） |

ノート配色は単色を基準とする。`バリエーション1`はトラック番号を既存の6色パレットへ循環して
割り当てる。既存プロジェクトの選択値を維持するため、この2方式の値とGUI位置は変更しない。

旧`Syncroh2_Filter_PianoRoll.dpr`が参照していた13方式を、現在の共通Colorユニットへ移植した。

| 配色単位 | 移植したバリエーション |
| --- | --- |
| トラック | DOS／虹／淡色／暗色 |
| MIDIキー（音階） | DOS／虹／淡色／暗色 |
| 階名（12半音周期） | ドレミ：虹／淡色／暗色 |
| MIDIキーの2色補間 | グラデ色：RGB／HSV |

トラック方式は楽曲内の最小～最大トラック番号、音階方式とグラデーションは楽曲内の最低～最高
MIDIキーを基準にする。表示音域が追従しても同じノートの色は変化しない。グラデーションは旧仕様どおり
低音側を`グラデ色2`、高音側を`グラデ色1`とし、1音だけの場合は`グラデ色1`を使う。
色計算は縦横へ重複させず、ノート、発音中鍵盤、放射光のすべてが同じ解決結果を使用する。

`サイズ`は多数の寸法項目を個別に変更せず、次の基準値を一度に設定する。

| サイズ | 鍵盤の長さ | 鍵盤の太さ | ノート太さ |
| --- | ---: | ---: | ---: |
| 中 | 120px | 40px | 80% |
| 大 | 180px | 60px | 80% |

従来の大を現在の中とし、大は中の1.5倍とする。拍線、小節線、発音線、鍵盤境界線は
追加のGUI項目を設けず、旧基準の鍵盤の太さ20pxを1倍として同じ倍率で線幅を拡大する。
選択リストを変更しただけでは個別値を上書きせず、`サイズ適用`ボタンを押したときだけ
AviUtl2の選択中オブジェクトへ`鍵盤の長さ・鍵盤の太さ・ノート太さ`をまとめて設定する。

各映像コールバックでAviUtl2が保持する項目値をローカルの`TPianoRollDisplaySettings`へコピーし、
範囲外値を防御的に補正してから表示実装へ渡す。複数オブジェクトの並列描画間で可変設定レコードを
共有しない。

## 共通カラーパレット

RGBA色型と既定色は`Source\Common\Color\SYNC_PianoRoll_Colors.pas`へ集約する。
RGBAキャンバス、表示設定、縦表示はこの共通色型を使用し、表示タイプ内へRGB定数を直接記述しない。

GUIへ公開する色は白鍵、黒鍵、白鍵／黒鍵レーン、拍線、小節線、発音線の7項目とする。
鍵盤境界色とトラック番号別の6色も共通パレットで管理するが、項目数を抑えるため現時点ではGUIへ
公開しない。色項目はAviUtl2 SDKの`B,G,R,X`配置で保持するが、`X`は予約領域であるため
描画アルファには使わない。GUIからRGBだけを取得し、レーンや線の透明度は共通パレットの既定値を維持する。

## 音楽ファイル解析とキャッシュ

Filterの`音楽ファイル`項目から元音楽ファイルを直接参照する。対応候補はSongReaderが扱う
`.mid`、`.midi`、`.ust`、`.vsq`、`.vsqx`、`.musicxml`、`.mxl`、`.xml`、`.mscx`、`.mscz`。
解析入口には内部保存形式用の`TSongData.LoadFromFile`ではなく、元音楽ファイル用の
`TSongData.LoadFromMusicFile`を使用する。

解析後は`TSongData`の可変オブジェクトをそのまま公開せず、ノート、拍、トラック、曲長を
`IPianoRollMusicData`の読み取り専用スナップショットへコピーする。ノートは開始秒、終了秒、
MIDIノート番号、ベロシティ、トラック番号、歌詞を保持する。拍は秒位置、番号、テンポを保持し、
トラックは番号と名称を保持する。

キャッシュキーには正規化した絶対パスを使用し、更新日時またはファイルサイズが変わった場合だけ
再解析する。同じファイルを指定した複数Filterは同じスナップショットを参照する。
キャッシュ置換後も既存の参照が解放されるまでスナップショットを保持できるよう、インターフェースで
参照カウントする。キャッシュ検索、生成、置換は排他して行う。

ファイル不存在、未知形式、破損、解析失敗、ファイルI/O例外は無効として扱い、画面表示や例外送出を
行わず映像を無加工で通す。無効な既存ファイルも更新日時とサイズが変わるまで再解析しない。

## 最小ピアノロール描画

現在は旧VCL描画系を移植せず、SDKの`SetImageData`へ透明RGBAバッファを直接渡す。
出力サイズはFilter対象の`Object_.Width`と`Object_.Height`を使用するため、仮想素材名で必要な寸法を
指定する。例えば1280×720、600秒、30fpsの描画ベースは次の名前にする。

```text
1280_720_600_30_1.syncpianoroll
```

縦表示では実効音域の低音を左、高音を右へ配置する。
発音位置は時間方向の80%とし、白い水平線で表示する。発音位置から時間方向の表示端までを
`表示時間`（初期値4秒）として座標へ変換し、未来のノートが上から発音位置へ流れる。
終了秒が未確定のノートは0.2秒として扱う。
現在時刻を過ぎて終了したノートは描画しない。ノート色は既定でGUI指定の単色を使用し、
バリエーション1ではトラック番号ごとの6色を使用する。

発音中のノートに対し、該当鍵盤をノートと同じ配色で点灯する。鍵盤点灯は発音開始以上、
終了時刻未満を対象とし、終了時刻が未確定のノートは0.2秒間点灯する。通常の白鍵・黒鍵には
暗い外縁、四辺の明暗、薄い反射帯を重ねて浅い立体感を付ける。発音中の鍵盤も同じ形状を維持し、
ノート色へ置き換えたうえで反射帯を通常鍵盤より強くする。縦表示は鍵盤の左側、横表示は上側へ
反射を置き、それぞれの時間方向と音階方向に合わせて専用描画する。現時点では追加GUI項目を設けず、
既存の鍵盤表示へ標準適用する。発音中の白鍵を描いた後は隣接黒鍵を復元し、最後に発音中の黒鍵を
重ねる。通常時と同じ鍵盤の重なりを維持し、点灯した白鍵が黒鍵を覆って大きく見えないようにする。

発音位置の光は発音開始から0.35秒間だけ表示し、時間経過とともに拡散しながら消す。
RGBAキャンバスの`BlendRadialGlow`は中心からの距離に応じてアルファを滑らかに減衰させ、
中心だけを白へ近づける。縦表示では発音線より未来側、横表示では発音線より未来側へ中心を少し移し、
鍵盤点灯と円が一体化して見える状態を抑える。この打鍵表示は縦／横の既存スタイルで標準有効とし、
追加のGUI項目は設けない。

実機確認で初期修正版の光が弱く狭かったため、発光半径は発音開始時に鍵盤の太さの2.4倍とし、
消灯へ近づくほど最大3.2倍まで広げる。空間減衰は外光が急激に消えないsmoothstep方式とし、
時間方向の明るさも平方根カーブで緩やかに落とす。再調整後の実機表示はやや大きいものの
許容範囲であることを確認済みとし、現在値を採用する。

初回試作では一定アルファの楕円を3枚重ねていたため、外周が硬い三重円となり、
光ではなく丸いマーカーに見えていた。発光画素と発音中判定、SDKの`R,G,B,A`配置には問題がなく、
主原因はAviUtl2へ渡す前の表現方法だった。現在の修正版は連続減衰と時間減衰へ変更済みだが、
最終的な見た目は実機上で再確認する。

横表示では実効音域の高音を上、低音を下へ配置する。鍵盤は発音位置の左側へ伸ばし、
未来のノートは発音位置から右へ流す。共通の`発音位置`は未来側に確保する時間方向の割合として扱うため、
初期値80%では発音線を画面左から20%の位置へ垂直に描画する。縦表示の座標処理は呼び出さず、
横表示ユニットが同じ共通設定、音楽データ、鍵盤判定、配色、RGBAキャンバスを独立して利用する。

鍵盤とノートの音階方向位置は、半音を単純に等分せず、白鍵1個を1.0とする共通音階座標で計算する。
`鍵盤の太さ`（初期値40px）は白鍵1個の音階方向サイズ、`鍵盤の長さ`（初期値120px）は
発音位置から時間方向へ伸びる白鍵の長さとする。黒鍵は白鍵間の中心へ太さ62%、長さ62%で重ねる。
背景レーンの音階方向の幅は鍵盤の白黒によって変えず、すべて黒鍵と同じ`鍵盤の太さ × 62%`とする。
ノートも同じ共通音階帯を基準に`共通音階帯の幅 × ノート太さ`で描画する。
背景レーンと各ノートは対応する白鍵または黒鍵の音階中心へ揃え、縦表示と横表示で同じ規則を使用する。
`鍵盤タイプ`の初期値は`標準ピアノ`とする。`ハープ（7音）`ではC、D、E、F、G、A、Bだけを
白鍵1個分の一定間隔と同一形状で配置し、黒鍵に相当する半音の鍵盤、背景レーン、流れるノート、
打鍵点灯、発音位置の光を描画しない。音域追従も半音ノートを対象へ含めない。
表示音域は固定ピクセルの`鍵盤の太さ`を維持し、`表示音階数`と`中央ノート`から決定する。
表示音階数は白鍵数ではなく、黒鍵を含む連続MIDIノート数とする。奇数個では中央ノートを範囲中央へ置き、
偶数個では中央ノートを上側の中央として扱う。MIDI範囲0～127の端へ達した場合は範囲全体を内側へ移し、
指定音階数を可能な限り維持する。

ユーザー設定から求める範囲を`基準表示音域`、各フレームの座標計算に使用する範囲を`実効表示音域`として
分離する。音域追従は`追従しない`を基本とし、`常に追従`と`はみ出したとき追従`を選択できる。
追従対象は現在発音中の全ノートとし、発音中のノートがなければ`表示時間`内で最も近い未来の
同時発音ノートを先読みする。それより遠い無音区間では基準表示音域へ戻す。

`常に追従`は対象ノートの最低音と最高音の中央へ実効音域の中央を合わせる。
`はみ出したとき追従`は対象が基準音域内なら移動せず、はみ出した分だけ実効音域を必要最小限移動する。
和音が表示音階数より広い場合は両方式とも和音中央を優先する。どの方式でも表示音階数を維持し、
MIDI範囲0～127の端では音域全体を内側へ補正する。

発音位置より未来側には、白鍵レーンと黒鍵レーンを半透明で描画する。拍データはノートと同じ
時間座標へ変換し、通常拍を細い白線、小節先頭を太い橙線で描画する。現在のSongReaderが持つ拍データは
四分音符の通算インデックスで拍子記号を保持しないため、初期状態では4拍ごとを小節先頭として扱う。
将来拍子情報を解析データへ追加した場合は、固定値ではなく時刻ごとの拍子から小節を判定する。

描画バッファは`Object_.ID + Object_.EffectID`ごとのレンダーコンテキストへ保持する。
同じ出力サイズでは毎フレーム再利用し、寸法が変わった場合だけ再確保する。同じコンテキストの描画は
個別に排他し、異なるオブジェクトの描画を不要に直列化しない。Filter終了時に全バッファを解放する。

内部設定は`表示時間`、`発音位置`、`ずらし`、`表示音階数`、`中央ノート`、`音域追従`、`鍵盤タイプ`、
`鍵盤の長さ`、`鍵盤の太さ`、`ノート太さ`、`ノート配色`、`ノート単色`という
方向非依存の意味で保持する。配色判定は共通Colorユニットへ集約し、各表示タイプから再利用する。

## ビルド

Delphi 37.0、Win64のみを対象にし、DebugとReleaseを保つ。
出力先は `C:\ProgramData\aviutl2\Plugin\SYNC_PianoRoll` とする。
DebugではDLLとRSMを残し、Releaseではプラグイン拡張子へコピー後にDLLとRSMを削除する。

Input Debug Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\SYNC_PianoRoll\SYNC_PianoRoll_Input.dproj"" /t:Build /p:Config=Debug /p:Platform=Win64"
```

Filter Debug Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\SYNC_PianoRoll\SYNC_PianoRoll_Filter.dproj"" /t:Build /p:Config=Debug /p:Platform=Win64"
```

Input Release Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\SYNC_PianoRoll\SYNC_PianoRoll_Input.dproj"" /t:Build /p:Config=Release /p:Platform=Win64"
```

Filter Release Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\SYNC_PianoRoll\SYNC_PianoRoll_Filter.dproj"" /t:Build /p:Config=Release /p:Platform=Win64"
```

## 継続作業ルール

- コードを追加または変更するときは、実装だけで完了とせず、コメントルールに従ったコメントの追加・更新も同時に行う。
- 処理内容を変更した場合は、既存コメントが実装と一致しているか確認し、古くなったコメントを残さない。
- 完了した機能追加と検証結果は[HISTORY.md](HISTORY.md)へ記録し、`note.md`には現行仕様と今後の作業だけを残す。

### コメントルール

- `Source\Plugin`、`Source\Common`、`Source\Display`と自作テストの先頭に、そのファイルが担当する責務を日本語で簡潔に記載する。
- コードを読み直しただけでは分かりにくい設計理由、安全上の制約、並列処理、互換処理、フォールバック条件を説明する。
- 代入や分岐をそのまま言い換えるコメントは増やさず、処理の目的または理由を記載する。
- 同じ仕様に関係する本体、表示実装、テスト、文書のコメントをまとめて確認し、古い構成を示す説明を残さない。
- `Source\Lib`は移入ライブラリとSDK定義を置くため、責務コメントの必須対象外とする。既存コメントは原則維持する。

## 次の実装候補

現在の最小ピアノロール描画、3種類の音域追従、連続減衰方式の発音位置発光、
旧`Syncroh2_Filter_PianoRoll.dpr`に存在した全13種類のトラック別／MIDIキー別配色は実装・実機確認済み。
これらの現行仕様を基本として維持する。

### 3D Type1縦横の継続地点

従来「横」として接続していた3D実装は、音階X・時間Yの縦表示だったため、縦専用ユニットへ
改名して設定の`縦`へ正しく接続した。ピアノ／3D／Type1／横も、時間X・音階Yの専用実装を追加した。
初期状態は2Dと同じ正面向きのX-Y平面とし、カメラやオブジェクト回転だけで遠近を付ける。
`表示時間`を時間軸の縮尺、`表示時間 3D`を未来頂点の生成上限として使用する。
レーン、拍線、小節線、発音線、ノート、標準ピアノ／ハープ鍵盤を描画し、
白鍵、黒鍵、ノートの実Z厚みを個別設定できる。

縦の鍵盤は正面で黒鍵を白鍵のY範囲内へ短く重ねる。横の鍵盤は発音線から左へ伸ばし、
黒鍵を白鍵の左側へ寄せる。白鍵上面Zは固定し、
白鍵厚みを背面Zへ伸ばす。黒鍵は白鍵上面を底として手前Zへ立ち上げる。
白鍵とハープ鍵の内部境界には側面を作らず、方向ごとの鍵盤全体の外周と手前だけに側面を作る。
Release版は`C:\ProgramData\aviutl2\Plugin\SYNC_PianoRoll`へ配置済みで、全11テストと
実DLL設定テストがPASSしている。新規の横3DもAviUtl2実機で、鍵盤を左、未来ノートを右、
高音を上へ配置する座標と遠近表示を確認済みである。鍵盤とノートのY位置は共通音階中心へ
揃っており、現時点では追加のYオフセット設定を設けない。

再開時は、まず縦横の鍵盤を正面と複数のカメラ角度で実機確認する。追加調整が不要なら、
次の候補は次のとおり。

1. 3D Type1縦横へ発音中の鍵盤点灯・発光を追加するか検討する。
2. 円環／トンネル型または中央発音型を、ピアノ／3Dの次のTypeとして検討する。
