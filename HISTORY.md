# SYNC_PianoRoll HISTORY

完了した機能追加と主な検証結果を記録する。現行仕様と継続作業は[note.md](note.md)を参照する。

## 2026-07-24

- `SYNC_Motion`を参考にInput／Filterの最小プロジェクトを作成し、Debug・Release Win64のビルドとプラグイン登録表のロードを確認した。
- 旧`Syncroh2`のピアノロール実装を調査し、旧INI方式を移植せず、元音楽ファイルをFilterから直接解析する方針を決定した。
- InputからFilterへ絶対フレームを渡す共有メモリと、Object ID＋Effect ID別のコンテキスト管理を追加した。
- `SYNC_Motion`のSongReaderを導入し、読み取り専用音楽データのキャッシュ、再利用、ファイル変更時の再解析を実装した。
- 透明RGBAバッファへ縦方向のノートと発音位置線を描く最小レンダラーを実装した。
- 共通処理、Input／Filter入口、縦表示、横表示、外部ライブラリを責務別フォルダーへ分離した。
- 白鍵／黒鍵判定と音階座標を共通処理へ追加し、縦表示へ鍵盤、レーン、ノート、拍線、小節線を実装した。
- 表示時間、発音位置、ずらし、音域、鍵盤寸法、ノート太さ、レーン、拍線、小節拍数をFilter GUIへ接続した。
- 鍵盤、レーン、拍線、小節線、発音線、トラック色を共通カラーパレットへ分離し、GUIから色を設定できるようにした。
- 各段階で描画、音楽解析、キャッシュ、登録表を回帰テストし、Input／FilterのDebug・Release Win64ビルドを確認した。

## 2026-07-27

- ノート矩形へ暗い外縁、明暗の面取り、半透明の反射帯を重ねる疑似立体表示を追加した。
- Filter末尾へ`ノート立体表示`（`平面／立体`、初期値`立体`）を追加し、既存GUI項目の位置を維持した。
- 縦表示は左側、横表示は上側へ反射を置く専用描画とし、小さすぎるノートは平面表示へ戻すようにした。
- 通常の白鍵・黒鍵と発音中のノート色鍵盤へ、外縁、面取り、反射帯による立体表現を追加した。発音中は反射を少し強くする。
- 旧`Syncroh2_Filter_PianoRoll.dpr`のトラック、音階、ドレミ、RGB／HSVグラデーションに対応する全13配色を共通Colorへ移植した。
- 既存の`単色`と`バリエーション1`を維持し、Filter末尾へグラデーションの高音側・低音側の2色設定を追加した。
- 旧13配色の専用テストを追加し、既存9件と合わせた全10テスト、FilterのDebug／Release Win64ビルド、配置済みRelease版の実DLLテストがPASSした。
- Filter GUIの先頭を`音楽ファイル`、`サイズ`、`サイズ適用`、`表示タイプ`の基本設定順へ並べ替え、旧`サイズ初期値`関連の名称を簡略化した。
- 発音中の白鍵が隣接黒鍵を覆い、通常鍵盤より大きく見える問題を修正した。白鍵点灯後に黒鍵を復元し、発音中の黒鍵を最前面へ描く。
- 立体表示と平面表示を縦・横の描画テストへ追加し、全9テスト、FilterのDebug／Release Win64ビルド、配置済みRelease版の実DLLテストがPASSした。
- 縦／横表示へ標準の打鍵表示を追加し、発音中の鍵盤をノート色で点灯するとともに、発音位置へ半透明の発光を描画するようにした。
- 初回の硬い三重円が発光に見えない問題を調査し、AviUtl2転送前の描画方式が原因であることを確認した。
- 発光を距離による連続減衰へ変更し、発音開始から0.2秒間で拡散・減衰するようにした。鍵盤点灯はノートの発音中を維持する。
- 実機確認で発光が弱く狭かったため、半径を鍵盤の太さの2.4～3.2倍へ拡大し、空間減衰をsmoothstep、表示時間を0.35秒へ調整した。
- 再調整後の発光を実機確認し、やや大きいものの許容範囲として現在値を採用した。
- 修正後の全9テストがPASSし、FilterのDebug／Release Win64ビルドと配置済みRelease版の登録表ロード・実DLL設定テストを確認した。
- 音域追従を`追従しない`、`常に追従`、`はみ出したとき追従`の3方式へ拡張した。
- 発音中の和音を優先し、無音時は表示時間内の直近未来ノートを先読みする共通追従処理を追加した。
- `常に追従`は対象音域の中央へ合わせ、`はみ出したとき追従`は表示幅を維持して必要最小限だけ移動するようにした。
- 追従処理の和音、未来ノート、上下移動、MIDI範囲端を専用テストで検証し、既存8件を含む全9テストがPASSした。
- Input／FilterのDebug・Release Win64ビルドを確認し、配置済みRelease版で登録表ロードと3方式の実DLL設定テストがPASSした。
- AviUtl2実機上で3種類の音域追従を簡易確認し、縦／横表示とも問題なく動作することを確認した。
- ノート配色へ既定の`単色`と、固定6色を循環する`バリエーション1`を追加した。
- 縦表示から独立した横表示を追加し、鍵盤を左、未来ノートを右、高音を上、低音を下へ配置した。
- Filterをタイムラインへ直接配置できる単体映像フィルターとし、`Object_.Time`、`Object_.Width`、`Object_.Height`を同期時刻と描画範囲に使用した。
- 音域指定を`表示音階数`と`中央ノート`へ変更し、MIDI範囲0～127の端で表示範囲を補正する処理を追加した。
- 将来の音域追従に備え、基準表示音域と実効表示音域を分離し、追従方式の列挙型を追加した。
- `小・中・大`のサイズプリセットと一括適用ボタンを追加し、鍵盤寸法、ノート太さ、各描画線を連動して変更できるようにした。
- `.syncpianoroll`入力とFilterを組み合わせた配布用エイリアスを追加した。
- Input＋Filter方式で描画寸法、時刻、画像バッファが正しく渡されることを確認した。
- Input＋Filter方式で位置移動、回転、拡大率、透明度の標準変形を利用できることを実機確認した。
- 表示音階数の初期値をMIDI全域の128から半分の64へ変更し、中央ノートの初期値はMIDI範囲中央の64を維持した。
- サイズプリセットから小を削除し、従来の大を新しい中（120px／40px）、その1.5倍を新しい大（180px／60px）とした。初期寸法も中へ変更した。
- 自作のPlugin、Common、Display、Testsをコメント規則に沿って系統別に監査し、単体Filter専用という古い説明を両配置対応へ更新した。旧共有フレーム経路の位置付けと並列描画の理由も明記した。
- 最終段階の回帰テストは全8件がPASSし、FilterのDebug／Release相当DLLのビルド、配置済みRelease版のロード、プリセット適用を確認した。

## 2026-07-29

- Input＋Filter方式で、入力設定の`開始時間`がFilterの音楽同期へ反映されない問題を実機ログで調査した。
- Inputは開始時間込みのフレームを正しく受け取っていたが、Filterが共有値を参照せず`Object_.Time`だけを使用していたことを原因として確認した。
- 通常のメディアオブジェクトではInputフレームをObject ID＋Effect ID別に基準化して使い、単体フィルタオブジェクトでは従来どおりローカル時刻を使うよう修正した。
- Inputの再取得が省略された再描画はローカルフレーム差分で補間し、古い共有値を新規オブジェクトの基準へ採用しないよう更新時刻を検証するようにした。
- 開始時間2.7秒＝81フレーム、再描画時の次フレーム補間、単体Filterのローカル時刻維持を回帰テストへ追加し、全10テストがPASSした。
- 原因調査用の一時ログ処理を削除し、修正版で開始時間が反映されることをAviUtl2実機上で確認した。
- Input／FilterのRelease Win64ビルドと配置済みRelease版の登録表ロード・実DLL設定テストがPASSした。
- 背景レーンとノートの音階方向の幅を白鍵・黒鍵で共通化した。共通音階帯を従来の黒鍵と同じ`鍵盤の太さ × 62%`とし、ノートはさらに`ノート太さ`を反映するよう変更した。
- 共通音階帯の幅と音階中心の計算を縦・横表示で共有し、背景レーンと各ノートを対応鍵盤の中央へ揃えた。鍵盤自体の白鍵・黒鍵形状は従来どおり維持した。
- 白鍵・黒鍵の同一ノート幅と縦横それぞれの中心位置を回帰テストへ追加した。全10テストがPASSし、Input／FilterのDebug・Release Win64ビルドと配置済みRelease版の実DLLテストを確認した。
- Filterへ`鍵盤タイプ`選択を追加し、従来表示を初期値の`標準ピアノ`、自然音だけを扱う新方式を`ハープ（7音）`とした。
- ハープ表示ではC～Bの自然音を等間隔・同一形状で描き、半音の鍵盤、背景レーン、流れるノート、打鍵点灯、発音位置の光を表示しないようにした。音域追従からも半音ノートを除外した。
- 鍵盤タイプの初期値と選択肢、ハープの共通幅、半音除外、縦横描画、音域追従を回帰テストへ追加した。
- 全10テストがPASSし、Input／FilterのDebug・Release Win64ビルドと、配置済みRelease版での鍵盤タイプ登録・半音除外を確認した。
- 鍵盤色バリエーションは一般的な楽器表示としての必要性が低いため追加しない方針とし、3D表示を次の検討対象とした。
- `Aul2AudioView`の3D描画を参考に、AviUtl2の`DrawPoly`へXYZ色付き頂点を直接渡す`3D平面`表示を追加した。
- 音階をX軸、未来時間を奥行き方向とする傾斜平面へ、レーン、ノート、拍線、小節線、発音線を描画した。標準ピアノ／ハープ、音域追従、ずらし、配色等は既存設定を共有する。
- `DrawPoly`非対応時は縦表示へフォールバックする。3D頂点生成とハープの半音除外を専用テストへ追加し、全11テスト、Input／FilterのDebug・Release Win64ビルド、配置済みRelease版の実DLLテストがPASSした。
- 3D表示へ発音線から手前へ伸びる標準ピアノ／ハープの鍵盤本体を追加した。
- `白鍵3D厚み`、`黒鍵3D厚み`、`ノート3D厚み`を追加した。初期値`0`は上面だけの平面表示とし、正の値では法線方向へ押し出して暗色の4側面を自動生成する。
- 平面時と三種の個別押し出しを3D頂点テストへ追加し、全11テスト、Input／FilterのDebug・Release Win64ビルド、配置済みRelease版のGUI項目・実DLL設定テストがPASSした。
- 3D平面に内蔵していた35度の傾斜を削除し、音階X軸・時間Z軸の完全な平面へ変更した。視点と回転はAviUtl2側だけで調整する。
- 押し出し形状の奥側端面が上面へ重なって見える問題を修正した。奥側端面を生成せず、左右・手前側面の後に上面を描く順序へ変更した。
- 表示選択を`表示分類`、`描画方式`、`表示タイプ`、`方向`の4項目へ分離した。現在はピアノ、2D／3D、Type1、縦／横を選択でき、鍵盤タイプは標準ピアノ／ハープの独立設定として直後へ配置した。
- 実装済みのピアノ／3D／Type1／横だけを3D描画へ接続し、未実装の3D縦は2D縦へ安全にフォールバックするようにした。
- 4分類の選択肢、nil終端、3D横の`DrawPoly`経路、未実装3D縦の2Dフォールバックを実DLLテストへ追加した。全11テスト、FilterのDebug・Release Win64ビルド、配置済みRelease版の登録・設定テストがPASSした。
- 3D Type1横を床向きのX-Z平面から、初期状態で2Dと同じ正面向きになるX-Y平面へ変更した。音階をX、時間をY、白鍵・黒鍵・ノートの実厚みをZへ割り当てた。
- `表示時間 3D (秒)`を`表示時間 (秒)`の直後へ追加した。既存の表示時間を時間軸の縮尺、3D用表示時間を未来頂点の生成上限として分離し、初期値30秒、範囲0.1～600秒とした。
- 表示時間4秒では除外される10秒先のノートが、表示時間3Dを30秒にすると生成されることと、厚み0のノートがZ方向へ広がらないことを3D頂点テストで確認した。
- 厚み付き白鍵を鍵ごとの個別ボックスにしていたため、隣接鍵の共有境界へ重複側面が生成され、カメラ角度によって右側だけ隙間に見えていた原因を修正した。
- 白鍵とハープ鍵は鍵ごとの上面を維持しつつ、左右側面を鍵盤全体の最外周、手前側面を連続した1面として生成するよう変更した。黒鍵は従来どおり個別に押し出す。
- 連続する7白鍵へ厚みを付けても外周3面だけが追加されることを3D頂点テストで検証した。全11テスト、FilterのDebug・Release Win64ビルド、配置済みRelease版の登録・設定テストがPASSした。
- 白鍵と黒鍵を同じY範囲へ重ね、わずかなZ差だけで配置していたため、カメラ回転時に黒鍵が白鍵へ埋まって見える問題を修正した。
- 鍵盤の基準Yから白鍵を下、黒鍵を上へ伸ばし、Zを実厚み専用とした。白鍵最上端と黒鍵最下端が基準Yで接して重ならないことを3D頂点テストへ追加した。
- 修正後の全11テスト、FilterのDebug・Release Win64ビルド、配置済みRelease版の登録・設定テストがPASSした。
- 黒鍵を白鍵と反対のY方向へ分離した構成では、正面表示で黒鍵がレーン背景へ溶け込むことを確認し、正面配置を再修正した。
- 黒鍵を白鍵のY範囲内へ短く重ね、白鍵上面Zを固定して厚みを背面へ、黒鍵は白鍵上面を底として手前へ伸ばす構造に変更した。白鍵厚みを変更しても白鍵・黒鍵の上面位置が動かないことを頂点テストへ追加した。
- 再修正後の全11テスト、FilterのDebug・Release Win64ビルド、配置済みRelease版の登録・設定テストがPASSした。
- 3D Type1を「横」として接続していたが、実際の座標は音階X・時間Yの縦表示だったため、`Vertical3D`専用ユニットへ改名して設定の`縦`へ接続し直した。
- 時間X・音階Y・厚みZの`Horizontal3D`専用ユニットを追加し、未来ノートを右、鍵盤を発音線の左、高音を上へ配置する3D Type1横を実装した。
- Filterの方向分岐を縦横それぞれの3D専用描画へ接続し、両方向の座標軸と`DrawPoly`経路を回帰テストへ追加した。全11テスト、Input／FilterのDebug・Release Win64ビルド、配置済みRelease版の登録・設定テストがPASSした。
- 新規の3D Type1横をAviUtl2実機で確認し、鍵盤を左、未来ノートを右、高音を上へ配置する座標と遠近表示が意図どおりであることを確認した。鍵盤とノートのY位置は現行の共通音階中心で問題ないため、追加のYオフセット設定は設けない。
